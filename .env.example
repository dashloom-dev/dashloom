# Public, non-secret origin used for local callbacks and metadata overrides.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Provider credentials will be added as server-side secrets when the connector
# implementations land. Never place real credentials in this file.
