# Public, non-secret origin used for local callbacks and metadata overrides.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# All authentication and BYOK provider credentials
# are server-side secrets documented in .dev.vars.example. Never put them here.
