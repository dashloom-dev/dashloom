import type { Metadata } from 'next';
import Link from 'next/link';

export const metadata: Metadata = { title: 'Privacy — Dashloom Community', description: 'Default data handling in self-hosted Dashloom Community.', alternates: { canonical: '/privacy' } };

export default function PrivacyPage() {
  return <main className="content-page"><nav className="site-nav"><Link className="brand" href="/"><span className="brand-mark" aria-hidden="true"><i /><i /><i /></span><span>Dashloom</span></Link><div className="nav-actions"><Link className="text-link" href="/terms">Terms</Link><Link className="button button-small" href="/docs">Docs</Link></div></nav><article className="prose-shell"><div className="eyebrow"><span />Privacy</div><h1>Your deployment controls its data.</h1><p className="lead">Dashloom Community does not send product analytics telemetry by default. The self-hosting operator is responsible for its privacy notice, infrastructure, retention, model providers, and connected services.</p><h2>Stored data</h2><p>The deployment stores authentication records, workspace configuration, encrypted connector and BYOK credentials, normalized metrics, Agent evidence and outputs, reports, schedules, and audit events.</p><h2>External processing</h2><p>Agent runs send only bounded evidence and the user&apos;s question to the configured BYOK provider. Connected data providers receive only the requests required by their documented connector.</p><h2>Control</h2><p>Owners can revoke credentials, export portable product evidence, and delete the workspace. Portable export omits identities and secrets.</p><h2>Security</h2><p>Secrets are encrypted server-side and ingestion tokens are stored as hashes. Report vulnerabilities through the repository Security Policy.</p></article></main>;
}
