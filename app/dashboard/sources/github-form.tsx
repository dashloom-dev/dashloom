'use client';

import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';

export function GitHubForm({ products }: { products: Array<{ id: string; name: string }> }) { const router = useRouter(); const [pending, setPending] = useState(false); const [message, setMessage] = useState(''); async function connect(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); setMessage('Validating GitHub identity and repository access…'); const response = await fetch('/api/connectors/github', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify(Object.fromEntries(new FormData(event.currentTarget))) }); const result = await response.json() as { error?: string; connector?: { repository: string } }; setPending(false); setMessage(result.error || `${result.connector?.repository || 'Repository'} connected.`); if (response.ok) { (event.target as HTMLFormElement).reset(); router.refresh(); } } async function sync() { setPending(true); setMessage('Syncing repository activity and health…'); const response = await fetch('/api/sync/github', { method: 'POST' }); const result = await response.json() as { error?: string; written?: number }; setPending(false); setMessage(result.error || `${result.written || 0} GitHub metric points synchronized.`); if (response.ok) router.refresh(); }
  return <form className="connector-form" onSubmit={connect}><div className="settings-grid"><label>Connection name<input name="displayName" required placeholder="Acme GitHub" /></label><label>Product<select name="productId" required disabled={!products.length}>{products.map((product) => <option value={product.id} key={product.id}>{product.name}</option>)}</select></label><label>Repository<input name="repository" required placeholder="owner/repository" /></label><label>Fine-grained token<input name="token" type="password" autoComplete="new-password" required minLength={20} placeholder="github_pat_…" /></label></div><footer><small>Grant repository Metadata read and Contents read only. Dashloom stores no source code or commit author data.</small><div><button className="app-secondary" type="button" disabled={pending} onClick={sync}>Sync GitHub</button><button className="app-primary" disabled={pending || !products.length}>{pending ? 'Working…' : 'Connect GitHub'}</button></div></footer>{message && <p className="form-message" role="status">{message}</p>}</form>;
}
